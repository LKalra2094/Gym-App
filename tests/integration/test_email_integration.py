"""
Integration tests for email sending functionality in app.utils.email.
These tests verify that emails are actually sent using the resend library.
"""
import pytest
import asyncio
from app.utils import email

# Removed email sending tests as per request. 